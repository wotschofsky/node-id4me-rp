{{#if givenName }}
  <h1>Welcome, {{ givenName }}</h1>
{{/if}}
<table class="table">
  <tbody>
    <tr>
      <th>Email</th>
      <td>{{ email }}</td>
    </tr>
    <tr>
      <th>Full Name</th>
      <td>{{ name }}</td>
    </tr>
  </tbody>
</table>
